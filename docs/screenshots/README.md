# Demo screenshots

Files in this folder (for submission / rubric):

| File | What it shows |
|------|----------------|
| `ui-signal-lab.png` | Main UI: scenario form + run history + observability links |
| `grafana-login.png` | Grafana sign-in (`admin` / `admin` at `http://localhost:3020`) |
| `loki-explore.png` | Grafana **Explore** → **Loki** (log query UI) |
| `sentry-issue.png` | Sentry **Issues** after `system_error` (redact secrets if you re-capture) |

**Optional extras** (not in repo yet — add if you want a fuller story):

- `grafana-dashboard.png` — **Dashboards → Signal Lab** with charts populated after several runs  
- `metrics-signallab.png` — `http://localhost:3001/metrics` with `signallab_scenario_runs_total`  
- `swagger-api-docs.png` — `http://localhost:3001/api/docs`

See **`docs/SCORE_100.md`**.

**Submission:** `SUBMISSION_CHECKLIST.md` — tick screenshot boxes and link this folder or your GitHub tree:  
`https://github.com/anvar992626/Test/tree/main/docs/screenshots`
